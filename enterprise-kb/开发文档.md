# Open WebUI 二次开发 — 详细开发文档

> 基于 [open-webui/open-webui](https://github.com/open-webui/open-webui) v0.10.2 进行二次开发
> 开发者：guyang
> 开始日期：2026-07-20

---

## 目录

- [Phase 1：分块预览与手动调整](#phase-1分块预览与手动调整)
- [Phase 2：向量化进度可视化](#phase-2向量化进度可视化)
- [Phase 3：检索质量评估面板](#phase-3检索质量评估面板)
- [Phase 4：知识库版本管理](#phase-4知识库版本管理)
- [Phase 5：整合导航与联调测试](#phase-5整合导航与联调测试)
- [Phase 6：Prompt 管理增强](#phase-6prompt-管理增强)
- [Phase 7：多策略分块引擎](#phase-7多策略分块引擎)
- [Phase 8：多 Agent 工作流编排](#phase-8多-agent-工作流编排)
- [Phase 9：单元测试](#phase-9单元测试)
- [Phase 10：并发压力测试](#phase-10并发压力测试)

---

## Phase 1：分块预览与手动调整

**完成日期**：2026-07-20

**解决的问题**：用户上传文档后无法看到文档如何被分块，分块质量无法验证，切坏了也不知道。

**实现的功能**：
- 文档上传后自动展示所有分块（编号、内容预览、token 数、元数据）
- 支持合并相邻分块
- 支持在指定位置拆分量块
- 调整完成后重建向量索引

---

### 1.1 数据库设计

**新建表**：`knowledge_chunk`

**Alembic 迁移文件**：[a7b8c9d0e1f2_add_knowledge_management_tables.py](backend/open_webui/migrations/versions/a7b8c9d0e1f2_add_knowledge_management_tables.py)

```sql
CREATE TABLE knowledge_chunk (
    id            TEXT PRIMARY KEY,           -- UUID
    knowledge_id  TEXT NOT NULL,              -- 所属知识库 FK
    file_id       TEXT NOT NULL,              -- 所属文件 FK
    chunk_index   BIGINT NOT NULL,            -- 分块序号（0-based）
    content       TEXT NOT NULL,              -- 分块完整文本
    token_count   BIGINT,                     -- 估算 token 数
    meta          JSON,                       -- 元数据 {page, section_header, ...}
    content_hash  TEXT,                       -- SHA-256 去重/对比用
    created_at    BIGINT NOT NULL,
    updated_at    BIGINT NOT NULL,

    UNIQUE(file_id, chunk_index),
    FOREIGN KEY (knowledge_id) REFERENCES knowledge(id) ON DELETE CASCADE,
    FOREIGN KEY (file_id) REFERENCES file(id) ON DELETE CASCADE
);

CREATE INDEX ix_knowledge_chunk_knowledge_id ON knowledge_chunk(knowledge_id);
CREATE INDEX ix_knowledge_chunk_file_id ON knowledge_chunk(file_id);
```

> 同时创建了 Phase 2-4 的 4 张表（knowledge_processing_task, knowledge_batch_task, knowledge_relevance_judgment, knowledge_snapshot），详见迁移文件。

---

### 1.2 后端 API

**新增端点**（全部挂载在 `/api/v1/knowledge/{id}/` 下）：

#### POST `/{id}/chunks/preview` — 分块预览

不写入向量库，仅运行 loader + splitter 展示分块结果。

**请求体**：
```json
{
  "file_id": "xxx-xxx-xxx"
}
```

**返回**：
```json
[
  {
    "id": "uuid",
    "knowledge_id": "xxx",
    "file_id": "xxx",
    "chunk_index": 0,
    "content": "第一章内容...",
    "token_count": 245,
    "meta": {"page": 1, "section_header": "## 引言"},
    "content_hash": "sha256...",
    "created_at": 1234567890,
    "updated_at": 1234567890
  }
]
```

**处理流程**：
```
1. 查找 file_id → 获取文件路径
2. Storage.get_file() → 本地文件
3. build_loader_from_config() → Loader 加载文档（PDF/DOCX/...）
4. loader.aload() → 提取文本
5. MarkdownHeaderTextSplitter (可选) → 按标题分块
6. RecursiveCharacterTextSplitter / TokenTextSplitter → 按 chunk_size 分块
7. 写入 knowledge_chunk 表（先删旧记录，再插入新记录）
8. 返回分块列表给前端
```

**代码位置**：[backend/open_webui/routers/knowledge.py](backend/open_webui/routers/knowledge.py) — `preview_knowledge_chunks()` 函数

---

#### GET `/{id}/files/{file_id}/chunks` — 获取文件分块

查询 `knowledge_chunk` 表，按 `chunk_index` 升序返回。

**代码位置**：[backend/open_webui/routers/knowledge.py](backend/open_webui/routers/knowledge.py) — `get_file_chunks()` 函数

---

#### GET `/{id}/chunks/{chunk_id}` — 查看单个分块详情

返回完整的 `content` 字段。

---

#### POST `/{id}/chunks/merge` — 合并分块

**请求体**：
```json
{
  "knowledge_id": "xxx",
  "file_id": "xxx",
  "start_index": 3,
  "end_index": 5
}
```

**处理流程**：
```
1. 读取 chunk_index 在 [start, end] 范围内的分块
2. 内容拼接（用 \n\n 分隔）
3. 创建新分块插入（chunk_index = start_index）
4. 删除原有分块
5. 后面的分块重新编号（chunk_index -= (end - start)）
```

> **约束**：只能合并相邻分块（chunk_index 连续），前端会校验。

---

#### POST `/{id}/chunks/split` — 拆分分块

**请求体**：
```json
{
  "chunk_id": "xxx",
  "split_at": 200     // 在第 200 个字符处拆分
}
```

**处理流程**：
```
1. 读取 chunk_id
2. 在 split_at 位置切分为 part_a 和 part_b
3. 原分块更新为 part_a
4. 插入新分块 part_b（chunk_index = 原 index + 1）
5. 后续分块下标 +1
```

> 约束：split_at 必须在 1 到 content.length-1 之间，且两部分都非空。

---

#### POST `/{id}/chunks/reindex` — 重建向量

**处理流程**：
```
1. 读取 knowledge_chunk 表中该 KB 的所有分块
2. 删除该 KB 在 ChromaDB 中的旧集合
3. 将分块转为 LangChain Document 列表
4. 调用 save_docs_to_vector_db() 重新生成向量 + 入库
```

---

### 1.3 自动集成：文件添加入库时自动写分块

在 `add_file_to_knowledge_by_id()` 端点中（文件第 1441 行附近），`process_file()` 完成后自动从 ChromaDB 查询该文件的向量记录，转换为 `knowledge_chunk` 行。

**代码位置**：[backend/open_webui/routers/knowledge.py](backend/open_webui/routers/knowledge.py) — `add_file_to_knowledge_by_id()` 函数内

**容错**：分块写入失败不会影响文件入库主流程（try/except + log warning）。

---

### 1.4 前端

#### 路由结构

```
src/routes/(app)/workspace/knowledge/[id]/
├── +layout.svelte          ← Tab 导航栏（Files│Chunks│Processing│Evaluate│Snapshots）
├── +page.svelte            ← KnowledgeBase 组件（Files 视图）
└── chunks/
    └── +page.svelte        ← ChunkManager 组件
```

#### 组件：ChunkManager.svelte

**位置**：[src/lib/components/workspace/Knowledge/ChunkManager.svelte](src/lib/components/workspace/Knowledge/ChunkManager.svelte)

**功能分区**：
```
┌──────────────────────────────────────────────────┐
│ ← Back to KB    Chunk Manager    [Preview] [Reindex] │
├──────────────────────────────────────────────────┤
│ 选择文件: [下拉框]                                  │
├──────────────────────────────────────────────────┤
│ [Merge Selected (2)]      125 chunks total        │
├────┬──────┬────────────────────┬────────┬────────┤
│ #  │ ☑   │ Preview            │ Tokens │ Actions│
├────┼──────┼────────────────────┼────────┼────────┤
│ 0  │ ☐   │ 第一章内容...       │ ~245   │View Split│
│ 1  │ ☑   │ 第二章内容...       │ ~180   │View Split│
│ 2  │ ☑   │ 第三章内容...       │ ~320   │View Split│
│ ...│ ...  │ ...                │ ...    │ ...    │
└────┴──────┴────────────────────┴────────┴────────┘
```

**交互流程**：
1. 下拉框选文件 → 自动加载已有分块（若无则需点 Preview 生成）
2. 勾选相邻分块 → Merge 按钮高亮 → 点击合并
3. 点 Split → 弹窗输入拆分位置 → 确认拆分
4. 点 View → 弹窗展示完整分块文本
5. 调整完成后点 Reindex → 重建向量

#### 组件：Tab 导航 (+layout.svelte)

**位置**：[src/routes/(app)/workspace/knowledge/[id]/+layout.svelte](src/routes/(app)/workspace/knowledge/[id]/+layout.svelte)

- 5 个 Tab：Files / Chunks / Processing / Evaluate / Snapshots
- 根据当前 URL 自动高亮对应 Tab
- 点击跳转到对应子页面
- 通过 SvelteKit 的 `<slot />` 渲染子页面内容

#### API 客户端函数

**位置**：[src/lib/apis/knowledge/index.ts](src/lib/apis/knowledge/index.ts)

新增 6 个函数：
```typescript
previewKnowledgeChunks(token, id, fileId)   // POST chunks/preview
getFileChunks(token, id, fileId)            // GET files/{fileId}/chunks
getChunkDetail(token, id, chunkId)          // GET chunks/{chunkId}
mergeKnowledgeChunks(token, id, fileId, start, end)  // POST chunks/merge
splitKnowledgeChunk(token, id, chunkId, splitAt)     // POST chunks/split
reindexKnowledgeChunks(token, id)           // POST chunks/reindex
```

---

### 1.5 涉及的文件清单

| 文件 | 改动类型 | 说明 |
|---|---|---|
| `backend/open_webui/models/knowledge.py` | **修改** | 新增 KnowledgeChunk 等 10 个模型类 + Pydantic forms |
| `backend/open_webui/routers/knowledge.py` | **修改** | 新增 6 个分块 API + 导入语句 + add_file_to_knowledge 自动写分块 |
| `backend/open_webui/migrations/versions/a7b8c9d0e1f2_*.py` | **新建** | Alembic 迁移（创建 5 张表） |
| `src/routes/(app)/workspace/knowledge/[id]/+layout.svelte` | **新建** | Tab 导航栏 |
| `src/routes/(app)/workspace/knowledge/[id]/chunks/+page.svelte` | **新建** | Chunk 管理页面路由 |
| `src/lib/components/workspace/Knowledge/ChunkManager.svelte` | **新建** | 分块管理主组件 |
| `src/lib/apis/knowledge/index.ts` | **修改** | 新增 6 个分块 API 客户端函数 |

---

### 1.6 验证方法

1. 打开 http://127.0.0.1:8080 → 进入任意知识库
2. 点击 **Chunks** Tab
3. 下拉选择文件 → 点击 **Preview / Refresh** 按钮
4. 确认分块表格正常展示（编号、内容预览、token 数）
5. 点 **View** 查看完整分块内容
6. 勾选两个相邻分块 → 点 **Merge Selected** → 确认合并
7. 点 **Split** → 输入拆分位置 → 确认拆分
8. 点 **Reindex Vectors** → 确认向量重建

---

## Phase 2：向量化进度可视化

**完成日期**：2026-07-20

**解决的问题**：用户批量上传文档后无法知道处理进度，文件是卡住了还是正在处理，完全不可见。

**实现的功能**：
- 每个文件独立展示处理状态（pending → chunking → embedding → completed / failed）
- 实时进度条（百分比 + chunks 进度）
- SSE (Server-Sent Events) 实时推送 + 3 秒轮询兜底
- 整体进度汇总（已完成/总数/失败数）
- 处理失败时展示错误信息

---

### 2.1 数据库设计

**表**：`knowledge_processing_task`（已在 Phase 1 迁移中创建）

```sql
CREATE TABLE knowledge_processing_task (
    id              TEXT PRIMARY KEY,
    knowledge_id    TEXT NOT NULL,              -- FK → knowledge.id
    file_id         TEXT,                       -- FK → file.id（允许为空）
    task_type       TEXT NOT NULL,              -- 'chunking' | 'embedding' | 'full_process'
    status          TEXT NOT NULL,              -- 'pending'|'chunking'|'embedding'|'completed'|'failed'
    progress_pct    BIGINT NOT NULL DEFAULT 0,  -- 0-100
    chunks_total    BIGINT,                    -- 总分块数
    chunks_processed BIGINT,                   -- 已处理分块数
    error_message   TEXT,                       -- 失败原因
    created_at      BIGINT NOT NULL,
    updated_at      BIGINT NOT NULL,

    FOREIGN KEY (knowledge_id) REFERENCES knowledge(id) ON DELETE CASCADE,
    FOREIGN KEY (file_id) REFERENCES file(id) ON DELETE CASCADE
);
```

**表**：`knowledge_batch_task`（预留，批量任务汇总）

```sql
CREATE TABLE knowledge_batch_task (
    id              TEXT PRIMARY KEY,
    knowledge_id    TEXT NOT NULL,
    total_files     BIGINT NOT NULL,
    files_processed BIGINT NOT NULL DEFAULT 0,
    total_chunks    BIGINT,
    chunks_embedded BIGINT DEFAULT 0,
    status          TEXT NOT NULL,              -- 'running' | 'completed' | 'failed'
    created_at      BIGINT NOT NULL,
    updated_at      BIGINT NOT NULL
);
```

---

### 2.2 后端 API

#### GET `/{id}/progress` — 获取处理状态

返回该知识库下所有文件的处理任务列表。

**返回示例**：
```json
[
  {
    "id": "uuid-1",
    "knowledge_id": "kb-xxx",
    "file_id": "file-xxx",
    "task_type": "full_process",
    "status": "embedding",
    "progress_pct": 50,
    "chunks_total": 120,
    "chunks_processed": 60,
    "error_message": null,
    "created_at": 1234567890,
    "updated_at": 1234567890
  }
]
```

#### GET `/{id}/progress/stream` — SSE 实时推送

使用 Server-Sent Events 每秒推送一次最新状态。

**响应格式**：`text/event-stream`
```
data: [{"id":"...","status":"embedding","progress_pct":50,...}]

data: [{"id":"...","status":"embedding","progress_pct":75,...}]

data: [{"id":"...","status":"completed","progress_pct":100,...}]
```

**终止条件**：所有任务进入 `completed` 或 `failed` 状态后自动关闭连接。无活动 5 分钟后超时。

#### GET `/{id}/progress/batch` — 批量任务进度

返回最近一次批量任务的汇总状态（预留接口）。

---

### 2.3 进程内集成

在 `add_file_to_knowledge_by_id()` 函数中插入钩子：

```
文件入库流程：
  1. _update_processing_progress(status='embedding', progress=50)
  2. process_file() → 实际处理
  3. _update_processing_progress(status='completed', progress=100)
  
  如果异常：
  4. _update_processing_progress(status='failed', error_message=str(e))
```

**辅助函数**：`_update_processing_progress(knowledge_id, file_id, status, ...)`
- 自动创建或更新 `knowledge_processing_task` 记录
- 同时写入 DB（持久化）+ 内存字典 `_progress_store`（SSE 快速读取）

**代码位置**：[backend/open_webui/routers/knowledge.py](backend/open_webui/routers/knowledge.py)

---

### 2.4 前端

#### 路由

```
src/routes/(app)/workspace/knowledge/[id]/processing/
└── +page.svelte          ← ProcessingDashboard 组件
```

#### 组件：ProcessingDashboard.svelte

**位置**：[src/lib/components/workspace/Knowledge/ProcessingDashboard.svelte](src/lib/components/workspace/Knowledge/ProcessingDashboard.svelte)

**UI 结构**：
```
┌──────────────────────────────────────────────────┐
│ Processing Status                    [Live ●]     │
│ Real-time document processing progress           │
├──────────────────────────────────────────────────┤
│ Overall                          3/5 completed    │
│ ██████████████░░░░░░░░ 60%                       │
├──────────────────────────────────────────────────┤
│ file-abc...  [Embedding]             50%          │
│ ██████████░░░░░░░░░░  60/120 chunks              │
│                                                   │
│ file-def...  [Completed]             100%         │
│ ██████████████████████  80/80 chunks              │
│                                                   │
│ file-ghi...  [Failed]                 0%          │
│ ░░░░░░░░░░░░░░░░░░░░░░  ⚠ Connection timeout     │
├──────────────────────────────────────────────────┤
│ [Refresh]  [← Back to Files]                     │
└──────────────────────────────────────────────────┘
```

**数据获取策略**（双通道）：
1. **主通道**：每 3 秒轮询 `GET /{id}/progress`
2. **辅助通道**：SSE 连接 `GET /{id}/progress/stream`，建立成功则即时更新

这样 SSE 连不上时（比如浏览器限制），轮询依然能工作。

#### API 客户端

**位置**：[src/lib/apis/knowledge/index.ts](src/lib/apis/knowledge/index.ts)

新增函数：
```typescript
getProcessingProgress(token, id)   // GET {id}/progress
getBatchProgress(token, id)        // GET {id}/progress/batch
```

---

### 2.5 涉及的文件清单

| 文件 | 改动类型 | 说明 |
|---|---|---|
| `backend/open_webui/routers/knowledge.py` | **修改** | 新增 `_update_processing_progress()` 辅助函数 + 3 个进度 API + add_file 钩子 |
| `src/routes/(app)/workspace/knowledge/[id]/processing/+page.svelte` | **新建** | 进度页面路由 |
| `src/lib/components/workspace/Knowledge/ProcessingDashboard.svelte` | **新建** | 进度仪表板组件（SSE + 轮询 + 进度条 + 状态标签） |
| `src/lib/apis/knowledge/index.ts` | **修改** | 新增 2 个进度 API 客户端函数 |

---

### 2.6 验证方法

1. 打开 http://127.0.0.1:8080 → 进入知识库 → 点击 **Processing** Tab
2. 确认初始状态显示 "No processing tasks yet"
3. 回到 Files Tab → 上传一个新文件并添加到知识库
4. 切换到 Processing Tab → 观察进度实时变化
   - 状态从 embedding → completed
   - 进度条从 50% → 100%
5. 刷新页面 → 确认任务记录仍然存在（DB 持久化）

---

## Phase 3：检索质量评估面板

**完成日期**：2026-07-20

**解决的问题**：无法评估知识库的检索质量——检索结果相关吗？召回率多少？没有人知道。

**实现的功能**：
- 输入测试查询 → 返回 Top-K 检索结果 + 相似度分数
- 标注每个结果「相关 ✓」或「不相关 ✗」
- 基于标注自动计算 recall@K、precision@K、MRR 指标
- 查询历史记录 + 标注持久化

---

### 3.1 后端 API

#### POST `/{id}/evaluate/query` — 执行评估查询

调 `query_collection()` 检索 → 返回格式化结果 + 指标

#### POST `/{id}/evaluate/annotate` — 标注检索结果

Upsert 标注到 `knowledge_relevance_judgment` 表

#### GET `/{id}/evaluate/judgments` — 查看历史标注

按 query_text 分组返回

#### DELETE `/{id}/evaluate/judgments/{query_text}` — 删除标注

---

### 3.2 关键技术细节

**调用 `query_collection` 的正确方式**（问题 #11）：
```python
result = await query_collection(
    request=request,
    collection_names=[id],           # list, not str
    queries=[form_data.query],        # list, not str
    embedding_function=request.app.state.EMBEDDING_FUNCTION,  # required
    k=k,
)
# 返回格式: {'documents': [[]], 'distances': [[]], 'metadatas': [[]]}
```

**指标计算**（服务端 Python）：
- `recall@K` = 检索到的相关文档数 / 总相关文档数
- `precision@K` = 检索到的相关文档数 / K
- `MRR` = max(1 / rank_of_relevant) 跨所有标注

---

### 3.3 前端

**路由**：`workspace/knowledge/[id]/evaluate/+page.svelte`

**组件**：[src/lib/components/workspace/Knowledge/EvaluatePanel.svelte](src/lib/components/workspace/Knowledge/EvaluatePanel.svelte)

**UI 结构**：
```
┌──────────────────────────────────────────────────┐
│ Retrieval Evaluation              ← Back to Files │
├──────────────────────────────────────────────────┤
│ [输入查询...]              [K=10 ▼] [Search]      │
├──────────────────────────────────────────────────┤
│ ┌─────────┐ ┌─────────┐ ┌────────┐ ┌──────────┐ │
│ │ 0.5000  │ │ 0.4000  │ │ 1.0000 │ │    2     │ │
│ │Recall@10│ │Prec@10  │ │  MRR   │ │Relevant  │ │
│ └─────────┘ └─────────┘ └────────┘ └──────────┘ │
├──────────────────────────────────────────────────┤
│ #1 score:0.8923  Transformer架构...  [✓] [✗]    │
│ #2 score:0.7841  大语言模型的发展...  [✓] [✗]    │
│ #3 score:0.6520  RAG技术结合...       [✓] [✗]    │
└──────────────────────────────────────────────────┘
```

---

## Phase 4：知识库版本管理

**完成日期**：2026-07-20

**解决的问题**：增删文件后无法回退到之前的知识库状态，配置错误无法撤销。

**实现的功能**：
- 创建快照（保存当前所有文件关联 + 分块记录的元数据）
- 快照卡片列表（标签、文件数、分块数、时间）
- 一键回滚（清除当前状态 → 恢复快照中的文件关联 → 重新向量化）
- 两个快照差异对比（新增/删除/修改的文件）
- 删除快照

---

### 4.1 后端 API

#### POST `/{id}/snapshots` — 创建快照

收集 `knowledge_chunk` 表中所有分块 + `knowledge_file` 关联 → JSON 序列化存入 `snapshot_data`。

#### GET `/{id}/snapshots` — 快照列表

按时间倒序返回。

#### POST `/{id}/snapshots/{sid}/rollback` — 回滚

1. 清除当前 chunk 记录和 file 关联
2. 从 snapshot_data 恢复分块元数据
3. 恢复文件关联（仅恢复元数据，向量需通过 Chunks Tab 手动 Reindex）
4. 返回恢复统计 + 提示信息

**设计权衡**（问题 #14）：快照只保存 file_id 引用而非文件内容。如果文件被物理删除（默认 `delete_file=True`），回滚无法恢复。为此新增 **Unlink Only** 按钮（`?delete_file=false`），只取消关联保留文件，使回滚可正常工作。

#### POST `/{id}/snapshots/compare` — 对比

对比两个快照的 file_id 集合差异，检查同一 file_id 的 content_hash 是否变化。

#### DELETE `/{id}/snapshots/{sid}` — 删除

---

### 4.2 前端

**路由**：`workspace/knowledge/[id]/snapshots/+page.svelte`

**组件**：[src/lib/components/workspace/Knowledge/SnapshotManager.svelte](src/lib/components/workspace/Knowledge/SnapshotManager.svelte)

**功能**：快照卡片网格 + 创建弹窗 + 回滚确认 + Compare 面板（三列对比）

---

## Phase 5：整合导航与联调测试

**完成日期**：2026-07-20

**内容**：
- `[id]/+layout.svelte` → 5 个 Tab 的路由导航
- 构建验证：`npm run build` 通过
- 后端导入验证：所有新模块 import 通过
- 端到端测试：创建知识库 → 上传文件 → 分块预览 → 进度监控 → 评估查询 → Unlink Only → 创建快照 → 回滚
- **附加改进**：在文件菜单新增 "Unlink Only" 按钮（`?delete_file=false`），解决快照回滚时源文件被物理删除的问题

---

## Phase 6：Prompt 管理增强

**完成日期**：2026-07-21

**解决的问题**：知识库 RAG 查询时使用系统默认 prompt，用户无法自定义检索上下文的注入方式。

**实现的功能**：
- 每个知识库可配置自定义的 RAG Prompt 模板
- 支持变量替换：`{query}`、`{context}`、`{kb_name}`
- 在知识库设置页面直接编辑和预览
- 评估模块自动读取 KB 关联的 Prompt 模板

---

### 6.1 数据库变更

在 `knowledge` 表的 `meta` JSON 字段中新增 `rag_prompt_template` 键（无需新表）：

```json
{
  "rag_prompt_template": "你是一个知识库助手。请根据以下参考资料回答用户问题。\n\n参考资料：\n{context}\n\n用户问题：{query}\n\n如果参考资料中没有相关信息，请如实告知。"
}
```

### 6.2 后端 API

#### PATCH `/{id}/prompt` — 更新 Prompt 模板
#### GET `/{id}/prompt` — 获取当前模板

### 6.3 聊天管道集成

在 `utils/middleware.py` 的 `apply_source_context_to_messages()` 中加钩子：

```
RAG 注入流程：
1. 检索文档 → 生成 context
2. 遍历 sources，检查是否是知识库类型
3. 如果是 KB，查 KB.meta 中是否有 rag_prompt_template
4. 有自定义模板 → 用自定义的（{query}/{context}/{kb_name} → [query]/[context]/KB名）
5. 无自定义 → 用全局 rag.template（系统设置）
6. 调用 rag_template() 拼成最终 prompt
```

**代码位置**：[utils/middleware.py](backend/open_webui/utils/middleware.py) — `apply_source_context_to_messages()` 函数

### 6.4 前端

### 6.3 前端

在评估面板（Evaluate Tab）中新增可折叠的 "RAG Prompt Template" 配置区：
- 文本编辑区（textarea），支持多行编辑
- 变量提示（`{query}`, `{context}`, `{kb_name}`）
- Reset to Default 按钮
- Save 按钮（PATCH 写入 KB meta）

**当前状态**：完整闭环已打通 ✅
1. 知识库 Prompt 模板的存储、编辑、API 读写 ✅
2. 聊天管道接入：`apply_source_context_to_messages` 在注入 RAG 上下文时自动检查 KB 是否有自定义模板 ✅
3. 变量转换：`{query}`→`[query]`、`{context}`→`[context]`、`{kb_name}`→KB 名称 ✅
4. 自定义模板优先，无自定义时回退全局 `rag.template` ✅

**面试应答**：
> "Prompt 模板的完整闭环已实现——从 Evaluate 页面编辑保存，到聊天时自动检测 KB 的自定义模板并注入。改 Prompt 后立即可在聊天中验证效果。同一知识库配不同 Prompt，LLM 回答风格完全不同。"

---

## Phase 7：多策略分块引擎

**完成日期**：2026-07-21

**解决的问题**：原有分块只有单一的 LangChain 通用分块，无法针对不同文档类型选择最优策略。

**实现的功能**：
- 7 种分块策略可选（Naive/General/Book/Paper/Resume/Table/QA）
- 关键词自动提取（jieba + 词频降级）
- 示例问题自动生成
- 前端 Method 下拉框切换，Preview 即时对比

---

### 7.1 分块策略详解

| 策略 | 原理 | 适用场景 |
|---|---|---|
| Naive | 固定字符数切片 + overlap | 纯文本，无结构 |
| General | LangChain 段落感知（RecursiveCharacterTextSplitter） | 通用文档 |
| Book | 正则识别"第X章"等章节标题 | 书籍、小说 |
| Paper | 关键词匹配摘要/引言/方法/实验/结论 | 学术论文 |
| Resume | 关键词匹配个人信息/教育/工作/技能等模块 | 简历 |
| Table | 检测 CSV `,` 或 Markdown `\|`，拆为键值对 | 数据表格 |
| QA | 正则匹配 Q:/A: 或 问题:/答案: 模式 | FAQ 文档 |

### 7.2 后端

**文件**：[utils/chunking_strategies.py](backend/open_webui/utils/chunking_strategies.py) — 纯函数，无 DB 依赖

**API 参数**：`POST /chunks/preview` body 新增 `method` 字段（默认 `general`）

### 7.3 前端

- ChunkManager 新增 **Method** 下拉框（7 选项）
- 分块表格新增 **Keywords** 列（蓝色标签）
- 分块表格新增 **Questions** 列（首个生成问题）

---

## Phase 7：多策略分块引擎

**状态**：已完成（2026-07-21）

集成 7 种分块策略 + jieba 关键词提取 + 问题自动生成。详见 `utils/chunking_strategies.py`。

---

## Phase 8：多 Agent 工作流编排

**完成日期**：2026-07-22

**状态**：✅ 已完成

**目标**：在知识库管理后台中加入轻量级 Agent 工作流编排，实现多 Agent 协作的配置化调度。

### 8.1 功能设计

新增 **Agents** Tab，提供可视化工作流配置：

```
工作流配置面板：
┌─────────────────────────────────────────┐
│ 步骤 1: [检索员 ▼] 绑定知识库: [test ▼]   │
│   描述: 从指定知识库检索相关文档            │
│   输出变量: {search_results}              │
├─────────────────────────────────────────┤
│ 步骤 2: [分析员 ▼] 输入: {search_results} │
│   描述: 分析检索结果，提炼关键信息           │
│   绑定 Prompt: [严谨分析模板 ▼]             │
│   输出变量: {analysis}                    │
├─────────────────────────────────────────┤
│ 步骤 3: [汇报员 ▼] 输入: {analysis}       │
│   描述: 生成最终报告                       │
│   输出: 最终输出                           │
├─────────────────────────────────────────┤
│ [+ 添加步骤]  [执行工作流]  [保存为模板]     │
└─────────────────────────────────────────┘
```

### 8.2 数据库设计

**新增表**：`agent_workflow` + `agent_workflow_step`

```sql
agent_workflow: id, user_id, name, description, created_at
agent_workflow_step: id, workflow_id, order_index, agent_role,
  knowledge_id(FK可选), prompt_template, input_var, output_var
```

### 8.3 API 端点

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/workflows` | 列出工作流 |
| POST | `/workflows` | 创建工作流 |
| GET | `/workflows/{id}` | 获取工作流详情 |
| PUT | `/workflows/{id}` | 更新工作流 |
| POST | `/workflows/{id}/execute` | 执行工作流（SSE 流式） |
| DELETE | `/workflows/{id}` | 删除工作流 |

### 8.4 Agent 角色预设

| 角色 | 默认 Prompt | 可选绑定 |
|---|---|---|
| 检索员 | "从知识库中检索与问题相关的文档" | 知识库 |
| 分析员 | "分析以下内容，提炼关键信息" | Prompt 模板 |
| 汇报员 | "基于分析结果生成结构化报告" | Prompt 模板 |
| 校验员 | "验证以下结论是否与原文一致" | 知识库 |
| 翻译员 | "将内容翻译为目标语言" | 无 |

### 8.5 执行流程（已实现 LLM 真实调用）

```
用户输入问题
  → 步骤1: 检索员 → query_collection() → 检索结果（含文档名+原文）
  → 步骤2: 分析员 → DeepSeek API.generate(检索结果) → 分析报告
  → 步骤3: 汇报员 → DeepSeek API.generate(分析结果) → 结构化报告+引用来源
  → SSE 实时推送每个步骤的执行结果
```

**关键技术细节**：
- 检索员输出包含文档名（通过 `Files.get_file_by_id()` 查询）、原文内容
- LLM 调用通过 `httpx` 直接调用 DeepSeek Chat Completions API
- API Key 从 `.env` 通过 `dotenv.load_dotenv(override=True)` 加载
- 变量传递：步骤间的 output_var → input_var 通过 prompt 模板替换实现

### 8.5.1 检索增强（2026-07-31 优化）

**问题**：ChromaDB 中每个 chunk 平均仅 200 字符，k=3 检索只拿到 ~600 字碎片，校验员无法验证分析结论。

**解决方案**：
- 检索量从 k=3 提升到 k=15
- 按 `file_id` 分组，组内按 `chunk_index` 排序后拼接为连贯文档
- 全链路提权截断值：文档 4000 字、变量传递 6000 字、prompt 10000 字、LLM 输出 2048 tokens
- 校验员提示词重写为 5 步核查流程（✅一致/❌不一致/⚠️未提及 + 原文引用 + 总体评分）

### 8.5.2 Word 报告下载（2026-07-31）

新增 `POST /_workflows/exec/download` 端点：
- 执行完成后生成 `.docx` 文件（含标题、查询问题、时间戳、各步骤完整输出）
- 使用 `python-docx` 生成，服务端内存缓存 1 小时自动清理
- 前端执行完成后显示"📥 下载 Word 文档"按钮，点击触发浏览器下载

### 8.6 简历价值

加入 Phase 8 后，简历可写：

> "设计并实现了多 Agent 工作流编排系统，支持 Agent 角色预设、知识库绑定、Prompt 模板注入、变量传递和 SSE 实时执行反馈。具备 Agent 协作调度、Task 编排、多个 LLM 角色分工的实战经验。"

---

## Phase 9：单元测试

**完成日期**：2026-08-14

**解决的问题**：核心分块逻辑、评估指标计算、快照 diff 等关键逻辑缺乏自动化测试，改动时无法快速回归验证。

**实现的功能**：
- 建立 4 层测试策略：L1 纯函数 / L2 抽取纯逻辑 / L3 路由接口 / L4 前端
- 编写 Claude Code 单元测试技能（`unit-test`），支持 pytest + vitest 双框架自动检测、测试编写、真实执行、报告生成
- 完成 L1：为分块引擎编写 32 个单元测试，全部通过

### 9.1 测试技能（Claude Code Skill）

文件：`.claude/skills/unit-test/SKILL.md`（项目根 `xm2/` 下，与 `open-webui/` 平级）

- 自动检测测试框架：Python 后端 → pytest（`backend/.venv/Scripts/python.exe -m pytest`）；TS 前端 → vitest（`npx vitest run`）
- 5 步工作流：定位目标 → 编写测试 → 真实执行 → 生成报告 → 失败修复
- 报告模板：概览表 + 用例明细 + 失败分析 + 结论建议
- 已实测验证的关键命令：pytest 在 `backend/` 下运行，`rootdir` 为 `open-webui/`（读取 `open-webui/pyproject.toml`）；`import open_webui.xxx` 路径可用；本环境未装 `pytest-asyncio`（已装插件 anyio/langsmith/docker）

### 9.2 L1：分块引擎单元测试

测试文件：`backend/tests/test_chunking_strategies.py`

被测目标：`backend/open_webui/utils/chunking_strategies.py`（纯函数，无 DB 依赖）

| 被测函数 | 用例数 | 覆盖点 |
|---|---|---|
| `chunk_document` | 5 | method 分发、未知 method 回退、`params=None`、7 种方法全可跑 |
| `naive_chunking` | 5 | 空串、单块、定长切分、overlap、start/end 元数据 |
| `general_chunking` | 6 | 空串、单段、短段合并、超限切分、overlap 保留末段 |
| `book_chunking` | 2 | 中文章节识别、无章节回退 |
| `paper_chunking` | 2 | section 识别、无章节回退 |
| `resume_chunking` | 2 | 模块识别、无模块回退 |
| `table_chunking` | 4 | CSV、列数对齐、Markdown、无表格回退 |
| `qa_chunking` | 2 | 问答对识别、无匹配回退 |
| `extract_keywords` | 2 | 空文本、返回类型/上限 |
| `generate_questions` | 2 | 精确计数、非空字符串 |

运行：`cd open-webui/backend && ./.venv/Scripts/python.exe -m pytest tests/test_chunking_strategies.py -v` → **32 passed in 0.14s**

### 9.3 测试中发现的问题

1. **`paper_chunking` 关键词脆弱**：section 判定为「关键词 in 行 且 行长 < 100」，关键词含「方法/结果/讨论」等高频词，正文含这些词且短于 100 字会被误判为 section 标题，导致 section 元数据错乱、切分点偏移。建议后续剔除高频词或改行首精确匹配。
2. **`naive_chunking` 非法参数死循环**：当 `chunk_size <= overlap`（如 `chunk_size=0`）时 `while` 无法收敛，会无限循环。需加参数校验。
3. **jieba 未安装导致分支未覆盖**：当前环境 `jieba` 未装（`JIEBA_AVAILABLE=False`），`extract_keywords` 只测到正则 fallback 分支，jieba 分支需在生产环境（装 jieba）下补测。

### 9.4 分层测试策略（后续规划）

| 层级 | 内容 | 状态 |
|---|---|---|
| L1 纯函数 | `chunking_strategies.py` | ✅ 完成（32 用例） |
| L2 抽取纯逻辑 | 评估指标 `compute_retrieval_metrics`、快照 diff `diff_snapshots`（当前内联在路由） | 待做 |
| L3 路由接口 | FastAPI TestClient + 内存 SQLite，需先补 `conftest.py` + `pytest-asyncio` | 待做 |
| L4 前端 | vitest 测 API 客户端 / 纯 TS 函数；组件需 `@testing-library/svelte` | 待做 |

---

## Phase 10：并发压力测试（Locust）

**完成日期**：2026-08-14

**解决的问题**：系统从未做过并发/性能验证，无法回答「100 人同时使用是否撑得住」。默认运行态是单 worker + SQLite，与真实生产环境（多 worker + PostgreSQL）差距大，潜在并发隐患从未暴露。

**实现的功能**：
- 搭建可复用的 Locust 压测体系（`loadtest/` 目录，与主项目平级、不侵入二次开发代码）
- Mock 外部 LLM / Embedding 调用，聚焦系统自身瓶颈（鉴权/DB/Chroma/RAG）
- 模拟生产环境：PostgreSQL（Docker）+ 4 worker uvicorn
- 4 类压测场景：①知识库读 ②聊天 SSE ③上传+向量化 ④混合全链路
- 100 并发保压 → 定位到 **ChromaDB 并发写损坏**这一核心瓶颈

---

### 10.1 压测架构

```
Locust ──► OWUI（4 worker，PostgreSQL） ──► Mock Server（127.0.0.1:9099）
              │                                  └─ /v1/models、/v1/chat/completions、/v1/embeddings
              └─► PostgreSQL（docker，127.0.0.1:5433）
              └─► ChromaDB（本地持久化）
```

**目录结构**（`loadtest/`）：

```
loadtest/
├── locustfile.py          # 汇总 4 个 User 类
├── config.py              # 目标地址 / KB id / 100 账号 / 上传文件
├── auth.py                # 登录 + token 缓存（on_start 登录一次）
├── sse.py                 # SSE 流式读取 + TTFB 单独上报
├── upload.py              # 上传+向量化两段式共享逻辑
├── scenarios/
│   ├── read_user.py       # ① 读接口
│   ├── chat_user.py       # ② 聊天 SSE
│   ├── upload_user.py     # ③ 上传 + 向量化
│   └── mixed_user.py      # ④ 混合全链路
├── mock/llm_mock.py       # Mock LLM/Embedding Server
├── seed.py                # DB 层 seeding：100 用户 + 测试文件
├── seed_kb.py             # HTTP 层 seeding：建库 + 灌文件
└── README.md              # 启动步骤 + 报告说明
```

### 10.2 环境准备（模拟生产）

| 组件 | 配置 | 说明 |
|---|---|---|
| PostgreSQL | Docker `postgres:16-alpine`，端口 5433，`max_connections=200` | 生产 DB |
| uvicorn | `--workers 4` | 模拟多 worker |
| Mock LLM | FastAPI，端口 9099 | 挡 DeepSeek/embedding |
| ChromaDB | 本地 PersistentClient | 首轮压测暴露并发写问题 |

### 10.3 压测结果（100 并发保压 ~8 分钟）

| 指标 | 数值 | 达标线 | 结论 |
|---|---|---|---|
| 总请求 / 失败 | 1371 / 36（2.63%） | <1% | ❌ |
| 峰值 RPS | 16.5（保压期掉到 ~3） | — | 吞吐低 |
| 平均响应时间 | 5033ms | — | 过载 |
| `KB.file_add` 失败率 | **51.92%（27/52）** | 0 | ❌ 核心瓶颈 |
| 读接口 P95 | ~3s | <300ms | ❌ |
| 聊天 / 读列表失败率 | <1% | <1% | ✅ |

**核心结论**：读接口和聊天基本扛住（失败率 <1%），塌方集中在写路径 `KB.file_add`。根因是 **ChromaDB PersistentClient 在多 worker 并发写下 HNSW 索引损坏**：

```
Error in compaction: Error constructing hnsw segment reader:
  Error creating hnsw segment reader: Error loading hnsw index
Error sending message to compactor
Error processing file: 0eae871e-...
```

### 10.4 关键技术发现

1. **ChromaDB PersistentClient 多进程写锁（核心）**：单 worker 开发模式没问题，切多 worker 后多个进程同时读写同一份 Chroma 本地文件（SQLite + HNSW），无跨进程锁，导致索引损坏、compactor 崩溃。**生产化必须切远程 Chroma**（`CHROMA_HTTP_HOST/PORT`），由服务端单进程串行化写入。

2. **内嵌 Chroma 吃满虚拟内存提交量**：4 个 worker 各内嵌一份 Chroma（HNSW 索引 memory-mapped），共吃掉 ~54 GB 虚拟内存 commit，把 Windows 提交限制（RAM + 分页文件）打满，导致 `import chromadb` 报 OpenBLAS 内存分配失败、bash fork 报 1455（ERROR_COMMITMENT_LIMIT）。停掉 OWUI 后 commit 从 73MB 回升到 54GB。

3. **读接口延迟 100 倍**：10 并发 ~30ms → 100 并发 ~3s，系统过饱和。需单独定位（PG 慢查询 / 连接池 / Chroma 锁争用），方案见「读接口慢二分定位」。

### 10.5 修复方案（已解决，已验证）

- 起独立 Chroma server（`chroma run --path <独立目录> --port 8000`），4 worker 通过 HTTP 连同一服务。
- `.env` 增加 `CHROMA_HTTP_HOST=127.0.0.1` / `CHROMA_HTTP_PORT=8000`。
- **验证结果**：复跑上传压测——10 并发 `KB.file_add` **0 失败**（之前 51.92%），Chroma 日志无 HNSW/compaction 错误；20 并发时暴露新瓶颈（mock + Windows select() 512 FD 上限），见问题记录 #31。

### 10.6 涉及文件清单

| 文件 | 说明 |
|---|---|
| `loadtest/`（全新目录） | Locust 压测脚本 + mock + seeding + README |
| `open-webui/.env` | 切 PostgreSQL、固定密钥、`RAG_OPENAI_*` 指向 mock |

> 压测体系独立于主项目，`loadtest/` 与 `open-webui/` 平级，不改动二次开发的功能代码。

---


---

## 附录：开发统计数据

| 指标 | 数据 |
|---|---|
| 新增数据库表 | 7 张 |
| 新增 API 端点 | 33 个（含 Word 下载） |
| 新增分块策略 | 7 种 |
| Agent 角色预设 | 5 种（检索员/分析员/汇报员/校验员/翻译员） |
| 新增前端页面 | 6 个页面（含 Agents Tab）+ 9 个组件 |
| 修改文件数 | 10 个后端 + 5 个前端 |
| 解决的问题 | 31 个（详见问题记录.md） |
| 上游 PR | 2 个 |
| 单元测试用例 | 32 个（L1 分块引擎） |
| 压测体系 | Locust 4 场景 + Mock LLM/Embedding + PostgreSQL 多 worker 模拟生产 |
| 开发周期 | 2026-07-20 ~ 2026-08-14 |

---

